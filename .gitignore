# Rust / Soroban
target/
**/*.rs.bk
*.wasm
**/test_snapshots/

# Node / circuits tooling
node_modules/

# Circuit build artifacts (large, regenerated). The public verification_key.json,
# vk_parsed.json, and vectors.json under circuits/build/ ARE committed.
circuits/build/*.r1cs
circuits/build/*.sym
circuits/build/*.zkey
circuits/build/*.ptau
circuits/build/withdraw_js/
# transient per-run demo data
circuits/build/integration.json

# Secrets and local config
.env
.env.*
!.env.example

# OS
.DS_Store
