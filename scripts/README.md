# scripts

Build and deploy helpers for circuits and Soroban contracts.

Planned:
- circuit build + trusted setup
- contract build (wasm) + deploy
- verifier initialization with the verification key
- pool creation via the factory
